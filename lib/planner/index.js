// lib/planner/index.js — STUB Fase 0 (implementação real nas fases seguintes).
const express = require('express');
module.exports = function make() { return { router: express.Router(), bridgeNotifications: () => {}, start: () => {} }; };
